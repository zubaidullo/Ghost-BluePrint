{
    "name": "${environmentName}",
    "description": "Ghost",
    "version": "1.24.7",
    "author": "https://github.com/Almansherov",
    "containers": [
        {
            "hostname": "${webContainerName}",
            "template": "debian-stretch",
            "peer-criteria": "HTTP-GROUP",
            "size": "${containerSize}",
            "port-mapping": [
                {
                    "protocol": "http",
                    "domain": "${domainName}",
                    "internal-port": "80",
                    "external-port": "80"
                }
            ]
        }
    ],
    "peer-criteria": [
        {
            "name": "HTTP-GROUP",
            "max-price": "25"
        }
    ],
    "ansible-configuration": {
        "source-url": "https://github.com/Almansherov/Ghost-BluePrint/archive/master.zip",
        "ansible-playbook": "Ghost.json",
        "extra-vars": [
            {
                "key": "Site_Title",
                "value": "${siteTitle}"
            }, 
            {
                "key": "User_Name",
                "value": "${domainName}"
            }, 
            {
                "key": "admin_user",
                "value": "${adminUser}"
            }, 
            {
                "key": "User_Password",
                "value": "${adminPwd}"
            }
        ],
        "groups": [
            {
                "name": "Ghost",
                "hostnames": [
                    "${webContainerName}"
                ]
            }
        ]
    },
    "user-variables": {
        "EnvironmentName": {
            "description": "Enter the environment name",
            "type": "string",
            "default": "Ghost",
            "validation": "[a-zA-Z0-9]+"
        },
        "DomainName": {
            "description": "Enter the application domain name",
            "type": "domain",
            "default": "change.the.domain",
            "validation": "[a-zA-Z0-9]+"
        },
        "SiteTitle": {
            "description": "Enter the site's title",
            "type": "string",
            "default": "title"
        },
        "UserName": {
            "description": "Site administrator user",
            "type": "string",
            "default": "admin"
        },
        "UserPassword": {
            "description": "Site administrator password",
            "type": "string",
            "default": "admin"
        },
        "WebContainerName": {
            "description": "Enter the container's hostname",
            "type": "string",
            "default": "Ghost",
            "validation": "[a-zA-Z0-9]+"
        },
        "ContainerSize": {
            "description": "Container Size",
            "type": "enum",
            "default": "TINY",
            "validation": "TINY,SMALL,MEDIUM,LARGE,HUGE"
        }
    }
}